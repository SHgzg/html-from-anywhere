# 邮件模板使用指南

本项目提供了多种风格的邮件模板，可以根据不同场景选择使用。

## 可用模板

### 1. Default (默认风格)
- **文件**: `default.html`
- **风格**: 经典蓝白配色
- **适用场景**: 日常通知、数据报告
- **特点**:
  - 蓝色头部 (`#007bff`)
  - 白色内容区
  - 灰色页脚
  - 圆角卡片设计

**预览**:
```bash
html-from-anywhere fetch \
  --url="https://api.example.com/data" \
  --output=email.html \
  --template=default
```

---

### 2. Minimal (简约风格)
- **文件**: `minimal.html`
- **风格**: 极简主义设计
- **适用场景**: 简洁通知、个人邮件
- **特点**:
  - 黑白灰色调
  - 大标题细字体
  - 底部分隔线
  - 无边框卡片

**预览**:
```bash
html-from-anywhere fetch \
  --url="https://api.example.com/data" \
  --output=email.html \
  --template=minimal
```

---

### 3. Business (商务风格)
- **文件**: `business.html`
- **风格**: 专业商务设计
- **适用场景**: 商务报告、正式通知
- **特点**:
  - 深色调（深蓝 `#2c3e50`）
  - 顶部装饰条
  - 左侧蓝色边框内容区
  - 深色页脚

**预览**:
```bash
html-from-anywhere fetch \
  --url="https://api.example.com/data" \
  --output=email.html \
  --template=business
```

---

### 4. Festival (节日庆典风格)
- **文件**: `festival.html`
- **风格**: 庆祝主题
- **适用场景**: 节日问候、活动通知
- **特点**:
  - 渐变紫罗兰色背景
  - 表情符号装饰（🎉✨🎊🎁）
  - 圆角渐变设计
  - 节日氛围

**预览**:
```bash
html-from-anywhere fetch \
  --url="https://api.example.com/data" \
  --output=email.html \
  --template=festival
```

---

### 5. Dark (暗色模式)
- **文件**: `dark.html`
- **风格**: 暗黑主题
- **适用场景**: 开发者通知、技术报告
- **特点**:
  - 黑色背景（`#1a1a1a`）
  - 绿色强调色（`#00ff88`）
  - 霓虹渐变装饰
  - 适合夜间阅读

**预览**:
```bash
html-from-anywhere fetch \
  --url="https://api.example.com/data" \
  --output=email.html \
  --template=dark
```

---

### 6. Card (卡片风格)
- **文件**: `card.html`
- **风格**: 现代卡片
- **适用场景**: 轻量通知、系统提醒
- **特点**:
  - 灰色背景（`#f5f7fa`）
  - 白色卡片
  - 阴影效果
  - 浅色页脚

**预览**:
```bash
html-from-anywhere fetch \
  --url="https://api.example.com/data" \
  --output=email.html \
  --template=card
```

---

## 模板对比

| 模板 | 配色风格 | 正式程度 | 最佳场景 |
|------|---------|---------|---------|
| **default** | 蓝白经典 | ⭐⭐⭐ | 日常通知 |
| **minimal** | 黑白简约 | ⭐⭐⭐⭐ | 个人邮件 |
| **business** | 深色商务 | ⭐⭐⭐⭐⭐ | 商务报告 |
| **festival** | 渐变多彩 | ⭐⭐ | 节日活动 |
| **dark** | 暗黑科技 | ⭐⭐⭐ | 技术报告 |
| **card** | 现代简洁 | ⭐⭐⭐⭐ | 系统通知 |

---

## 如何使用模板

### 命令行使用

```bash
html-from-anywhere [command] \
  --template=<模板名称> \
  --output=<输出文件>
```

### 示例

#### 1. 使用简约模板发送数据报告

```bash
html-from-anywhere fetch \
  --url="https://api.example.com/monthly-report" \
  --template=minimal \
  --subject="月度数据报告" \
  --output=report.html
```

#### 2. 使用商务模板发送重要通知

```bash
html-from-anywhere string \
  --data='{"title":"重要通知","content":"系统将于今晚22:00进行维护"}' \
  --template=business \
  --format=text \
  --output=notice.html
```

#### 3. 使用节日模板发送节日祝福

```bash
html-from-anywhere markdown \
  --content="祝大家新年快乐！🎉\n\n愿新的一年里身体健康、工作顺利！" \
  --template=festival \
  --subject="新年祝福" \
  --output=newyear.html
```

#### 4. 使用暗色模板发送开发者报告

```bash
html-from-anywhere fetch \
  --url="https://api.example.com/dev-metrics" \
  --template=dark \
  --subject="开发者周报" \
  --output=dev-report.html
```

---

## 自定义模板

如果内置模板不满足需求，你可以创建自己的模板：

### 创建自定义模板

1. 在 `templates/` 目录创建新文件 `custom.html`
2. 使用 Handlebars 语法：`{{variable}}` 和 `{{{content}}}`
3. 使用 `--template=custom` 指定你的模板

### 模板变量

可用变量：
- `{{subject}}` - 邮件主题
- `{{{content}}}` - 邮件内容（HTML，不转义）
- `{{year}}` - 当前年份
- `{{company}}` - 公司名称

### 模板示例

```html
<!DOCTYPE html>
<html>
<head>
  <title>{{subject}}</title>
</head>
<body>
  <h1>{{subject}}</h1>
  <div>{{{content}}}</div>
  <footer>&copy; {{year}} {{company}}</footer>
</body>
</html>
```

---

## 邮件客户端兼容性

所有模板都经过优化，兼容以下主流邮件客户端：

- ✅ Gmail
- ✅ Outlook (2007+)
- ✅ Apple Mail
- ✅ Yahoo Mail
- ✅ QQ 邮箱
- ✅ 163 邮箱
- ✅ 手机邮件客户端

### 技术要点

1. **表格布局** - 不使用 Flexbox/Grid
2. **内联样式** - 不使用外部 CSS
3. **标准 HTML** - 不使用实验性特性
4. **MSO 兼容** - 为 Outlook 添加特殊标签

---

## 模板开发建议

如果你想创建自己的模板，请注意：

1. **保持简洁** - 避免复杂的 CSS3 特性
2. **使用表格** - 用 `<table>` 进行布局
3. **内联样式** - 所有样式写在 `style` 属性中
4. **测试兼容性** - 在多个邮件客户端测试
5. **响应式** - 考虑移动端显示

---

## 快速测试

测试所有模板：

```bash
# 创建测试数据
echo '[{"name":"测试数据","value":"100"}]' > test.json

# 测试所有模板
for template in default minimal business festival dark card; do
  html-from-anywhere file \
    --path=test.json \
    --template=$template \
    --output=test-$template.html \
    --subject="测试模板 - $template"
done
```
